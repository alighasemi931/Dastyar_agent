# Example environment variables for local Docker / development
# Copy to .env and fill values before running

# Use a local sqlite file stored in the mounted ./data directory
DATABASE_URL=sqlite:///./data/dastyar.db

# OpenAI API key (required if you use OpenAI embeddings / LLMs)
OPENAI_API_KEY=your_openai_api_key_here

# Model name (optional)
MODEL=gpt-4o-mini
